{
  "name": "oracle-mirror-bff",
  "version": "0.1.0",
  "private": true,
  "description": "BFF for Oracle Mirror — hides OpenAI API key behind Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types test/random.selftest.ts",
    "test:stream": "node --experimental-strip-types test/openai-stream.selftest.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
